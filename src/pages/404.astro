---
// Any path other than "/" falls through to here while the site is paused,
// so every visitor sees the coming-soon page regardless of the URL they try.
import ComingSoon from '../components/ComingSoon.astro';
---

<ComingSoon />
